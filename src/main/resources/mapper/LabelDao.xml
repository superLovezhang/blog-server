<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyzz.blog.dao.LabelDao">

    <resultMap id="BaseResultMap" type="com.tyzz.blog.entity.Label">
        <!--@Table label-->
        <result property="labelId" column="label_id" jdbcType="INTEGER"/>
        <result property="labelName" column="label_name" jdbcType="VARCHAR"/>
        <result property="state" column="state" jdbcType="BOOLEAN"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          label_id, label_name, state, create_time, update_time
        from blog.label
        where label_id = #{labelId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          label_id, label_name, state, create_time, update_time
        from blog.label
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          label_id, label_name, state, create_time, update_time
        from blog.label
        <where>
            <if test="labelId != null">
                and label_id = #{labelId}
            </if>
            <if test="labelName != null and labelName != ''">
                and label_name = #{labelName}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="labelId" useGeneratedKeys="true">
        insert into blog.label(label_name, state, create_time, update_time)
        values (#{labelName}, #{state}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update blog.label
        <set>
            <if test="labelName != null and labelName != ''">
                label_name = #{labelName},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where label_id = #{labelId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from blog.label where label_id = #{labelId}
    </delete>

</mapper>