<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyzz.blog.dao.CommentDao">

    <resultMap id="BaseResultMap" type="com.tyzz.blog.entity.Comment">
        <!--@Table comment-->
        <result property="commentId" column="comment_id" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="parentId" column="parent_id" jdbcType="INTEGER"/>
        <result property="replyId" column="reply_id" jdbcType="INTEGER"/>
        <result property="articleId" column="article_id" jdbcType="INTEGER"/>
        <result property="state" column="state" jdbcType="BOOLEAN"/>
        <result property="like" column="like" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          comment_id, content, user_id, parent_id, reply_id, article_id, state, like, create_time, update_time
        from blog.comment
        where comment_id = #{commentId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          comment_id, content, user_id, parent_id, reply_id, article_id, state, like, create_time, update_time
        from blog.comment
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          comment_id, content, user_id, parent_id, reply_id, article_id, state, like, create_time, update_time
        from blog.comment
        <where>
            <if test="commentId != null">
                and comment_id = #{commentId}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="parentId != null">
                and parent_id = #{parentId}
            </if>
            <if test="replyId != null">
                and reply_id = #{replyId}
            </if>
            <if test="articleId != null">
                and article_id = #{articleId}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="like != null">
                and like = #{like}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="commentId" useGeneratedKeys="true">
        insert into blog.comment(content, user_id, parent_id, reply_id, article_id, state, like, create_time, update_time)
        values (#{content}, #{userId}, #{parentId}, #{replyId}, #{articleId}, #{state}, #{like}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update blog.comment
        <set>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="parentId != null">
                parent_id = #{parentId},
            </if>
            <if test="replyId != null">
                reply_id = #{replyId},
            </if>
            <if test="articleId != null">
                article_id = #{articleId},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="like != null">
                like = #{like},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where comment_id = #{commentId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from blog.comment where comment_id = #{commentId}
    </delete>

</mapper>