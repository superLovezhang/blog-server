<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyzz.blog.dao.NotificationDao">

    <resultMap id="BaseResultMap" type="com.tyzz.blog.entity.pojo.Notification">
        <!--@Table notification-->
        <result property="notificationId" column="notification_id" jdbcType="INTEGER"/>
        <result property="notifycationType" column="notifycation_type" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="state" column="state" jdbcType="BOOLEAN"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          notification_id, notifycation_type, content, state, update_time, create_time
        from blog.notification
        where notification_id = #{notificationId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          notification_id, notifycation_type, content, state, update_time, create_time
        from blog.notification
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          notification_id, notifycation_type, content, state, update_time, create_time
        from blog.notification
        <where>
            <if test="notificationId != null">
                and notification_id = #{notificationId}
            </if>
            <if test="notifycationType != null and notifycationType != ''">
                and notifycation_type = #{notifycationType}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="notificationId" useGeneratedKeys="true">
        insert into blog.notification(notifycation_type, content, state, update_time, create_time)
        values (#{notifycationType}, #{content}, #{state}, #{updateTime}, #{createTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update blog.notification
        <set>
            <if test="notifycationType != null and notifycationType != ''">
                notifycation_type = #{notifycationType},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where notification_id = #{notificationId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from blog.notification where notification_id = #{notificationId}
    </delete>

</mapper>