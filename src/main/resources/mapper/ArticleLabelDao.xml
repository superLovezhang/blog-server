<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.ArticleLabelDao">

    <resultMap id="BaseResultMap" type=".entity.ArticleLabel">
        <!--@Table article_label-->
        <result property="articleLabelId" column="article_label_id" jdbcType="INTEGER"/>
        <result property="articleId" column="article_id" jdbcType="INTEGER"/>
        <result property="categoryId" column="category_id" jdbcType="INTEGER"/>
        <result property="state" column="state" jdbcType="BOOLEAN"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BaseResultMap">
        select
          article_label_id, article_id, category_id, state
        from blog.article_label
        where article_label_id = #{articleLabelId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          article_label_id, article_id, category_id, state
        from blog.article_label
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="BaseResultMap">
        select
          article_label_id, article_id, category_id, state
        from blog.article_label
        <where>
            <if test="articleLabelId != null">
                and article_label_id = #{articleLabelId}
            </if>
            <if test="articleId != null">
                and article_id = #{articleId}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="articleLabelId" useGeneratedKeys="true">
        insert into blog.article_label(article_id, category_id, state)
        values (#{articleId}, #{categoryId}, #{state})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update blog.article_label
        <set>
            <if test="articleId != null">
                article_id = #{articleId},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
        </set>
        where article_label_id = #{articleLabelId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from blog.article_label where article_label_id = #{articleLabelId}
    </delete>

</mapper>