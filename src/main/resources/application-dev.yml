server:
    port: 8000
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
    dbcp2:
      　　min-idle: 5         #数据库连接池的最小维持连接数
      　　initial-size: 5     #初始化提供的连接数
      　　max-total: 5          #最大的连接数
      　　max-wait-millis: 200        #等待连接获取的最大超时时间
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password: # 如果未单独配置默认为空即可
    timeout: 1000
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: admin
#    password: admin
#    virtual-host: /
#    publisher-confirm-type: correlated #开启消息从生产者发送到exchange上的确认回调
#    publisher-returns: true #开启消息从exchange没有分配到queue上的确认回调
#    template:
#      mandatory: true #指定消息没有队列匹配到是发送回调还是直接丢弃
#    listener:
#      simple:
#        acknowledge-mode: manual #消费端消费消息手动确认
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl